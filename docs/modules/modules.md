# Modulverwaltung

**Zugriff:** Administratoren.

Zeigt auto-discoverte native Module, deren Metadaten und Aktivierungszustand. Ein Modul unter `app/Modules/` ist kein Beispielordner: Es wird als native Implementierung geprüft. Das ausführliche Lehrbeispiel liegt deshalb unter `examples/`.
