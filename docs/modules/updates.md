# Updates

**Zugriff:** Administratoren.

Prüft, bereitet vor und installiert offizielle ModulNest-Pakete aus `stable.json`. Downloads, SHA256-Prüfung, Backups, Wartungsmodus und Recovery arbeiten fail-closed.
