[pytest]
testpaths = tests/e2e
python_files = test_*.py
addopts =
    -ra
    --browser=chromium
    --output=.local/e2e/artifacts
    --tracing=retain-on-failure
    --video=retain-on-failure
    --screenshot=only-on-failure
markers =
    auth: requires login credentials for authenticated browser flows

